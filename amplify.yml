version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - pnpm config set ignore-scripts false
        - rm -rf node_modules
        - pnpm install --no-frozen-lockfile
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - /root/.local/share/pnpm/store/**/*
